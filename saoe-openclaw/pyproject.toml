[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "saoe-openclaw"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "saoe-core",
]

[tool.ruff]
line-length = 100
target-version = "py312"
